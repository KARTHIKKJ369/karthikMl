services:
  portfolio-ai:
    image: ghcr.io/ggml-org/llama.cpp:server
    container_name: karthik-portfolio-ai
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./checkpoints:/models
    command: >
      -m /models/karthik_qwen1.5b_q8.gguf
      --host 0.0.0.0
      --port 8000
      -c 2048
      -t 2
      --api-key ${PORTFOLIO_API_KEY:-kj_live_sec_789f2a4b1c}
      --alias karthik-qwen2.5-1.5b
      --metrics
